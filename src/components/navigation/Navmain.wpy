<style lang="less">
  @import "./Navmain.less";

</style>
<template>
  <view class="container">
    <view class="weui-grids">
      <view class="weui-grid" wx:for="{{navList}}" wx:key="name">
        <navigator url="{{item.url+'?name='+item.name+'&code='+item.code}}" data-url="{{item.url}}"  @tap="clickNavigator">
          <view class='weui-grid__bg'>
            <view class="weui-grid__icon">
              <image src=" {{item.icon}}" mode="scaleToFill" />
            </view>
            <text class="weui-grid__label">{{item.name}}</text>
          </view>
        </navigator>
      </view>
    </view>
  </view>
</template>
<script>
  import wepy from 'wepy'
  export default class Navmain extends wepy.component {
    props = {
      navList: {
        type: [Array],
        default: []
      }
    }
    data = {

    }
    methods = {
      clickNavigator(e) {
        let navUrl = e.currentTarget.dataset.url
        this.$emit('event-navmain', navUrl)
      }
    }
  }

</script>
